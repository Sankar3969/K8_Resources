apiVersion: V1
kind: Pod
metadata:
  name: nginx
  namespace: expense
  labels:
    project: expense
    Environment: dev
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerport: 80
    - name: almalinux
      image: almalinux:9
